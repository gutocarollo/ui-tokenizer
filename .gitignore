node_modules/
.DS_Store
*.log

# estado de execucao, nunca versionado
.claude/evidence/
.claude/runs/
.cache/
*.tmp

# segredos: allowlist e o unico modo seguro
.env
.env.*
**/settings.json
*.pem
id_rsa*

# Bytecode Python: artefato de execucao, nunca fonte. Dois .pyc estavam
# RASTREADOS ate 2026-07-31 — cada rodada do guard sujava o diff.
__pycache__/
*.pyc
# design_system_template.json — VERSIONADO desde 2026-08-01 (decisao do dono).
# Deixou de ser rascunho: carrega $law, $forbidden e $validation, e docs/log.md
# e o dossie da ui-ux-pro-max ja o citam. Ignorado, ele nao sobrevivia a um clone.
